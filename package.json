{
  "scripts": {
    "dev": "eleventy --serve",
    "eleventy": "eleventy --watch",
    "debug": "set DEBUG=* & eleventy",
    "build": "cross-env NODE_ENV=production eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0",
    "cross-env": "^7.0.3",
    "html-minifier": "^4.0.0"
  }
}
